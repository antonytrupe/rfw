import GameEngine from "@/GameEngine"
import Character from "../Character"
import EventEmitter from "events"
import Action from "./Action"
import BaseAction from "./Action"

test.skip("BaseAction", () => {
    // const action = { turn: 0, type: "moveToAction" }
    // const character = new Character({})
    // const engine = new GameEngine({ fps: 1, doGameLogic: true }, new EventEmitter)
    // //@ts-ignore
    // character.actions.push(new BaseAction({ character, engine, action }))

    // character.actions.forEach((action, i) => {
    //     if (!!action.do) {
    //         const a = new Action({ action })
    //         console.log(a)

    //         //new BaseAction({character:this,engine,action})
    //         //character.actions.splice(i, 1, a)
    //     }
    //     action.do({ character, engine, dt: 1, now: 1 })
    // })
})